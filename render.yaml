services:
  - type: web
    name: telegram-userbot-upgraded
    env: python
    plan: free
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    healthCheckPath: /healthz
